#lang info

;; ========================================
;; pkg info

(define collection "proofcheck")
(define version "1.0")

(define deps
  '("base"
    "parser-tools-lib"
    "rackunit-lib"
    "web-server-lib"))

;; ========================================
;; collect info

(define name "proofcheck")
